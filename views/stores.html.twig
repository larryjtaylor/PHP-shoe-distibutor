<html>
     <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" type="text/css" href="/css/styles.css">
        <link href="https://fonts.googleapis.com/css?family=Permanent+Marker" rel="stylesheet">
        <title>Shoe Stores</title>
    </head>
    <body>
        <div class="jumbotron">
            <h1 class="pageHeader">Shoes and Such</h1>
        </div>
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h1>Here are all of the shoe stores:</h1>

                    {% if stores is not empty %}
                    <ul>
                        {% for store in stores %}
                        <li><a href="/stores/{{ store.getId }}">{{ store.getStoreName }}</a></li>
                        {% endfor %}
                    </ul>
                    {% else %}
                    <p>There are no stores entered yet!</p>
                    {% endif %}
                </div>
                <div class="col-md-4">
                    <img src="/img/lillard.jpg" alt="Damian Lillard Sneakers" id="dame">
                    <h3 class="goHome"><a href="/">Go home</a></h3>
                </div>
                <div class="col-md-4">
                    <h4>Add a new store:</h4>
                    <form action="/stores" method="post">
                        <label for="store_name">Store name:</label>
                        <input type="text" name="store_name" id="store_name" placeholder="ex: Shoetopia" required><br>

                        <button type="submit" class="btn-success">Add it!</button>
                    </form>

                    <form action="/delete_stores" method="post">
                        <button type="submit" class="btn-danger">Remove all stores</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
